# heart-disease-prediction-system

<!-- # commands
# git status
# git add 01-into
# git commit -am 'read me update'
# git push   
# cp file a file b

#pip install pipenv
#pipenv install numpy scikit-learn==1.7.0 flask gunicorn
#pipenv shell
#pipenv run gunicorn --bind 0.0.0.0:9696 predict:app
#exit / ctr+d

#docker run -it --rm python:3.13.9-slim
#docker run -it --rm --entrypoint=bash python:3.13.9-slim
#docker build -t mlzoomcamp-app .
#docker run -it --rm mlzoomcamp-app
#docker run -it --rm -p 9696:9696 mlzoomcamp-app
-->